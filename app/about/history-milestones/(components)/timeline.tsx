import Milestones from "@/app/(landing)/(components)/milestones";
import { milestones } from "@/app/(landing)/page";





const Timeline = () => {
  return <Milestones milestones={milestones} title="Our Journey" />;
};

export default Timeline;
